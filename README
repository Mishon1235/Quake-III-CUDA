Program is comparing times of execution for few difrent types of algorithm which are calculating invers square root. In program there are algorithms which are using CUDA 
acceleration (GPU multicore abilities) but also Quake III fast invers square root alghoritm.
